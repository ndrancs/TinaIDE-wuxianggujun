/**
 * Convention plugin that provides maven publish functionality
 * This is a stub to satisfy the plugin requirement in sora-editor modules
 */

plugins {
    id("maven-publish")
}

// This plugin is just a placeholder to allow sora-editor modules to build
// We don't actually need to publish them to Maven
