; Code block patterns for C constructs

(struct_specifier
  body: (_) @scope.marked)

(union_specifier
  body: (_) @scope.marked)

(enum_specifier
  body: (_) @scope.marked)

(compound_statement) @scope.marked

(initializer_list) @scope.marked
